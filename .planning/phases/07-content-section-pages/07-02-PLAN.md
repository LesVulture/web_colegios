---
phase: 07-content-section-pages
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/lib/content/personalization.ts
  - src/app/personalizacion/page.tsx
  - src/app/cuellos/page.tsx
autonomous: true
requirements: [SECTION-02, SECTION-03]

must_haves:
  truths:
    - "La pagina /personalizacion muestra 4 opciones con imagen real (next/image), nombre y descripcion"
    - "La opcion 'estampacion-digital' muestra descripcion completa (no vacia)"
    - "Las 4 cards de personalizacion NO tienen texto introductorio — van directo al contenido"
    - "La pagina /cuellos muestra 4 color swatches con nombre, hex y codigo de producto"
    - "El swatch blanco tiene borde visible (no desaparece contra el fondo)"
    - "Las 2 tablas de tallas (Ninos y Adolescentes/Adultos) son simultaneamente visibles, sin tabs"
    - "Las notas comerciales son visibles en la pagina de cuellos"
    - "Breadcrumbs visibles en ambas paginas"
  artifacts:
    - path: "src/lib/content/personalization.ts"
      provides: "Fixed description for estampacion-digital"
      contains: "estampacion-digital"
    - path: "src/app/personalizacion/page.tsx"
      provides: "Complete Personalizacion page with 4 option cards and images"
      min_lines: 40
    - path: "src/app/cuellos/page.tsx"
      provides: "Complete Cuellos page with color swatches, size tables, and commercial notes"
      min_lines: 80
  key_links:
    - from: "src/app/personalizacion/page.tsx"
      to: "src/lib/content/personalization.ts"
      via: "PERSONALIZATION_OPTIONS import"
      pattern: "import.*PERSONALIZATION_OPTIONS.*from.*@/lib/content"
    - from: "src/app/cuellos/page.tsx"
      to: "src/lib/content/collars.ts"
      via: "COLLAR_DATA import"
      pattern: "import.*COLLAR_DATA.*from.*@/lib/content"
    - from: "src/app/cuellos/page.tsx"
      to: "src/lib/content/helpers.ts"
      via: "getTechnologyById for collar technology display"
      pattern: "getTechnologyById"
---

<objective>
Build the Personalizacion and Cuellos pages with production-quality layouts, real content from TypeScript data, and visually impressive presentation for B2B sales meetings.

Purpose: Replace the last 2 placeholder pages so the salesperson can present personalization options (with images) and collar specifications (colors, sizes, commercial notes) during meetings.
Output: Complete /personalizacion and /cuellos pages with real content, animations, and professional presentation.
</objective>

<execution_context>
@/Users/vulture/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vulture/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-content-section-pages/07-RESEARCH.md
@.planning/phases/07-content-section-pages/07-01-SUMMARY.md
@src/app/globals.css
@src/lib/content/personalization.ts
@src/lib/content/collars.ts
@src/lib/content/types.ts
@src/lib/content/helpers.ts
@src/components/breadcrumb.tsx
@src/components/tech-icon.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix personalization data and build Personalizacion page</name>
  <files>
    src/lib/content/personalization.ts
    src/app/personalizacion/page.tsx
  </files>
  <action>
**personalization.ts data fix**: The `estampacion-digital` option has `description: ''` (empty string). Add a descriptive text: `'Impresion de alta definicion sobre tela mediante tecnologia de sublimacion digital, ideal para disenos detallados y policromias complejas.'` per research recommendation. This is a data correction, not a model change.

**personalizacion/page.tsx**: Rewrite the placeholder page as a Server Component (NO 'use client'). Structure:

1. **Metadata**: Keep existing title. Update description to be more specific.

2. **Layout**: `mx-auto max-w-7xl px-4 lg:px-8 py-6 lg:py-10` (same container pattern).

3. **Breadcrumb**: `<Breadcrumb items={[{ label: 'Inicio', href: '/' }, { label: 'Personalizacion' }]} />`

4. **Page header**: Heading "Personalizacion de Uniformes" (h1, font-heading, 3xl/4xl). Per user decision, NO intro paragraph — go directly to the content grid.

5. **Grid**: `grid grid-cols-1 md:grid-cols-2 gap-6 lg:gap-8` for the 4 personalization options. This gives a 2x2 layout on desktop, 1-col on mobile — fits 4 items perfectly.

6. **Each card** (inline in page, no separate component):
   - Container: `group overflow-hidden rounded-lg border border-border bg-background transition-all duration-300 hover:shadow-lg hover:-translate-y-1` with `animate-fade-in-up` class and inline `style={{ animationDelay: \`${i * 100}ms\` }}`.
   - **Image**: Use `next/image` `<Image>` component with `src={option.image}`, `alt={option.name}`, `width={600}`, `height={400}`, `className="w-full aspect-[4/3] object-cover transition-transform duration-500 group-hover:scale-105"`. The fixed aspect ratio prevents uneven card heights (pitfall #2 from research).
   - **Text content**: Below image in a `p-6` container.
     - `h3` with option name: `font-heading font-semibold text-lg text-foreground`.
     - Description paragraph: `text-sm text-muted-foreground mt-2`.

7. **Imports needed**: `Image` from next/image, `{ PERSONALIZATION_OPTIONS }` from @/lib/content, `{ Breadcrumb }` from @/components/breadcrumb, `type { Metadata }` from next.

8. **Important**: NO intro text before the grid per user locked decision. NO stampingRoutes/printRoutes mapping per deferred decision.
  </action>
  <verify>Run `bun run build` — must complete with zero errors and /personalizacion route appears in the static pages output. Verify that personalization.ts `estampacion-digital` description is no longer empty.</verify>
  <done>The /personalizacion page displays 4 personalization options in a 2x2 grid with real images (next/image), names, and descriptions. All 4 descriptions are non-empty. Cards have hover effects (shadow, elevation, image zoom). Page has breadcrumb and staggered entry animation.</done>
</task>

<task type="auto">
  <name>Task 2: Build Cuellos page with color swatches, size tables, and commercial notes</name>
  <files>
    src/app/cuellos/page.tsx
  </files>
  <action>
**cuellos/page.tsx**: Rewrite the placeholder page as a Server Component (NO 'use client'). Structure:

1. **Metadata**: Keep existing title. Update description.

2. **Layout**: `mx-auto max-w-7xl px-4 lg:px-8 py-6 lg:py-10` (same container pattern).

3. **Breadcrumb**: `<Breadcrumb items={[{ label: 'Inicio', href: '/' }, { label: 'Cuellos' }]} />`

4. **Page header**: Heading "Cuellos y Punos" (h1, font-heading, 3xl/4xl). Below it, display material info (`COLLAR_DATA.material`) and guarantee (`COLLAR_DATA.guarantee`) as subtle info text.

5. **Technologies section**: Show the 3 associated technologies (desempeno, proteccion-solar, control-humedad) using `getTechnologyById()` and `TechIcon`. Render as a horizontal flex row with icon + name chips, similar to fabric detail page tech display. This shows the collar's technology credentials.

6. **Colors section** (h2: "Colores Disponibles"):
   - Grid: `grid grid-cols-2 md:grid-cols-4 gap-4` for the 4 colors.
   - Each color card: `flex items-center gap-3 rounded-lg border border-border p-4 transition-all duration-200 hover:shadow-md` with `animate-fade-in-up` stagger.
   - Color circle: `size-12 rounded-full border border-border shadow-sm` with inline `style={{ backgroundColor: color.hex }}`. The `border border-border` ensures white (#FFFFFF) swatch is visible against white background (pitfall #1 from research).
   - Name: `font-medium text-foreground`.
   - Product code: `text-xs text-muted-foreground` showing "Ref. {productCode}".

7. **Size tables section** (h2: "Tabla de Tallas"):
   - Display BOTH tables simultaneously (per user locked decision — NO tabs).
   - Use a `grid grid-cols-1 lg:grid-cols-2 gap-6 lg:gap-8` to show them side by side on desktop, stacked on mobile.
   - **Table 1 header**: "Ninos" (h3).
   - **Table 2 header**: "Adolescentes y Adultos" (h3).
   - Each table: HTML `<table>` with Tailwind styling matching the pattern from fabric detail specs table:
     - `w-full text-sm`
     - `<thead>`: `border-b border-border` rows, `py-3 text-left font-medium text-muted-foreground` cells
     - Columns: Talla, Cuello, Puno
     - `<tbody>`: `border-b border-border` rows, first cell `font-medium text-foreground`, other cells `text-muted-foreground`
   - Each table wrapped in a `rounded-lg border border-border overflow-hidden` container for visual polish.

8. **Commercial notes section** (h2: "Notas Comerciales" or "Informacion Importante"):
   - Display the 3 commercial notes from `COLLAR_DATA.commercialNotes`.
   - Render as a styled list or info cards. Suggested: `rounded-lg bg-surface p-6` container with `<ul>` using `list-disc list-inside space-y-2 text-sm text-muted-foreground` items. Use a subtle info-box style with a left border accent: `border-l-4 border-brand-primary`.

9. **Imports needed**: `{ COLLAR_DATA, getTechnologyById }` from @/lib/content, `{ TechIcon }` from @/components/tech-icon, `{ Breadcrumb }` from @/components/breadcrumb, `type { Metadata }` from next.

10. **Important**: Both tables visible simultaneously. No tabs, no accordion. White color swatch MUST have visible border.
  </action>
  <verify>Run `bun run build` — must complete with zero errors and /cuellos route appears in the static pages output.</verify>
  <done>The /cuellos page displays: (1) material and guarantee info, (2) 3 associated technology icons, (3) 4 color swatches with visible borders including white, (4) 2 size tables side-by-side (Ninos + Adolescentes/Adultos) with all rows and 3 columns each, (5) 3 commercial notes in a styled info box. Breadcrumb visible. All content from COLLAR_DATA rendered.</done>
</task>

</tasks>

<verification>
1. `bun run build` passes with zero errors
2. Both `/personalizacion` and `/cuellos` appear in SSG output (static icon, not lambda)
3. Personalizacion: 4 cards with real images, all descriptions non-empty
4. Cuellos: 4 color swatches visible (including white), 2 tables with correct row counts (4 + 6), 3 commercial notes displayed
5. Hover effects work on personalization cards and color swatches
</verification>

<success_criteria>
- /personalizacion shows 4 options in 2x2 grid with real images (next/image), names, descriptions (all non-empty)
- /cuellos shows color swatches (white visible), both size tables simultaneously, commercial notes
- Both pages have breadcrumbs, responsive layout, hover effects, staggered animations
- Build passes with zero errors, both pages are SSG
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-section-pages/07-02-SUMMARY.md`
</output>
