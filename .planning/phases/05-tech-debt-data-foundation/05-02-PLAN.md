---
phase: 05-tech-debt-data-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/content/types.ts
  - src/lib/content/technologies.ts
  - src/lib/content/personalization.ts
  - src/lib/content/collars.ts
  - src/lib/content/helpers.ts
  - src/lib/content/index.ts
autonomous: true
requirements:
  - DEBT-06
  - DATA-01
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "Los 3 iconos de tecnologia que faltaban (algodon, antimanchas, solidez-a-la-luz) muestran un icono Lucide representativo en lugar de un string vacio"
    - "El archivo de datos para Personalizacion existe con 4 opciones, cada una con id, nombre, descripcion y ruta de imagen"
    - "El archivo de datos para Cuellos existe con 4 colores (hex + nombre), 2 tablas de tallas, y notas comerciales"
    - "Cada tecnologia tiene un campo expandedDescription con contenido del PDF o igual a description si no hay texto expandido"
    - "La funcion getFabricsByTechnology(techId) devuelve las telas que usan esa tecnologia"
    - "Todos los nuevos modelos son importables desde @/lib/content con tipado correcto"
  artifacts:
    - path: "src/lib/content/types.ts"
      provides: "Interfaces PersonalizationOption, CollarColor, CollarSize, CollarData + Technology expandida"
      contains: "PersonalizationOption"
    - path: "src/lib/content/personalization.ts"
      provides: "PERSONALIZATION_OPTIONS array con 4 opciones del PDF p.15"
      contains: "PERSONALIZATION_OPTIONS"
    - path: "src/lib/content/collars.ts"
      provides: "COLLAR_DATA con colores, tallas y notas comerciales"
      contains: "COLLAR_DATA"
    - path: "src/lib/content/technologies.ts"
      provides: "14 tecnologias con expandedDescription e iconos Lucide fallback"
      contains: "expandedDescription"
    - path: "src/lib/content/helpers.ts"
      provides: "getFabricsByTechnology helper function"
      contains: "getFabricsByTechnology"
    - path: "src/lib/content/index.ts"
      provides: "Barrel exports para todos los nuevos modelos y tipos"
      contains: "PERSONALIZATION_OPTIONS"
  key_links:
    - from: "src/lib/content/personalization.ts"
      to: "src/lib/content/types.ts"
      via: "PersonalizationOption type import"
      pattern: "import type.*PersonalizationOption"
    - from: "src/lib/content/collars.ts"
      to: "src/lib/content/types.ts"
      via: "CollarData type import"
      pattern: "import type.*CollarData"
    - from: "src/lib/content/helpers.ts"
      to: "src/lib/content/fabrics.ts"
      via: "FABRICS import for inverse relationship"
      pattern: "getFabricsByTechnology.*FABRICS\\.filter"
    - from: "src/lib/content/index.ts"
      to: "src/lib/content/personalization.ts"
      via: "barrel re-export"
      pattern: "export.*personalization"
---

<objective>
Crear los modelos de datos completos para Personalizacion, Cuellos y Tecnologias expandidas, proveer iconos fallback Lucide para las 3 tecnologias sin icono, y agregar la funcion helper de relacion inversa tecnologia->telas.

Purpose: Entregar el data layer completo que alimentara la pagina de Personalizacion (Phase 7), la pagina de Cuellos (Phase 7), las fichas tecnicas con tooltips de tecnologia (Phase 6), y la pagina de Tecnologias (Phase 7).
Output: types.ts expandido, personalization.ts, collars.ts, technologies.ts enriquecido, helpers.ts con nueva funcion, index.ts actualizado.
</objective>

<execution_context>
@/Users/vulture/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vulture/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tech-debt-data-foundation/05-RESEARCH.md
@.planning/phases/05-tech-debt-data-foundation/05-CONTEXT.md

@src/lib/content/types.ts
@src/lib/content/technologies.ts
@src/lib/content/helpers.ts
@src/lib/content/index.ts
@src/lib/content/fabrics.ts
@Uniformes_Colegios.pdf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create type interfaces + personalization and collars data models</name>
  <files>src/lib/content/types.ts, src/lib/content/personalization.ts, src/lib/content/collars.ts</files>
  <action>
**Paso 1: Expandir types.ts con nuevas interfaces**

Agregar las siguientes interfaces a `src/lib/content/types.ts` (despues de las interfaces existentes):

```typescript
export interface PersonalizationOption {
  readonly id: string;
  readonly name: string;
  readonly description: string;
  readonly image: string;
}

export interface CollarColor {
  readonly id: string;
  readonly name: string;
  readonly hex: string;
  readonly productCode: string;
}

export interface CollarSize {
  readonly size: string;
  readonly collarMeasure: string;
  readonly cuffMeasure: string;
}

export interface CollarData {
  readonly material: string;
  readonly guarantee: string;
  readonly technologies: readonly string[];
  readonly colors: readonly CollarColor[];
  readonly sizes: {
    readonly children: readonly CollarSize[];
    readonly adolescentsAdults: readonly CollarSize[];
  };
  readonly commercialNotes: readonly string[];
}
```

Tambien expandir la interfaz `Technology` existente agregando el campo opcional:
```typescript
export interface Technology {
  readonly id: string;
  readonly name: string;
  readonly icon: string;
  readonly description: string;
  readonly expandedDescription?: string;  // Texto largo del PDF (si existe)
}
```

**Paso 2: Crear personalization.ts (DATA-01)**

Crear `src/lib/content/personalization.ts` con el contenido del PDF p.15. Las descripciones DEBEN ser texto literal del PDF, sin resumir ni adaptar (decision locked del usuario).

```typescript
import type { PersonalizationOption } from './types';

export const PERSONALIZATION_OPTIONS = [
  {
    id: 'dibujos-exclusivos',
    name: 'Dibujos Nuevos y Exclusivos',
    description: 'Creacion de disenos unicos para instituciones que requieren una tela diferente segun sus necesidades particulares.',
    image: '/images/content/page15-95.webp',
  },
  {
    id: 'estampacion-digital',
    name: 'Disenos de Alta Definicion con Estampacion Digital',
    description: '',  // PDF p.15 solo muestra titulo, sin descripcion adicional visible
    image: '/images/content/page15-94.webp',
  },
  {
    id: 'estampacion-davos',
    name: 'Estampacion Tipo Davos (Marca de Agua)',
    description: 'Proceso que por temperatura y presion graba un diseno sobre la tela.',
    image: '/images/content/page15-96.webp',
  },
  {
    id: 'desarrollo-color',
    name: 'Desarrollo de un Nuevo Color',
    description: 'Programacion de un color exclusivo que no se encuentre en el portafolio de linea vigente.',
    image: '/images/content/page15-93.webp',
  },
] as const satisfies readonly PersonalizationOption[];
```

**IMPORTANTE:** Antes de hardcodear el mapeo de imagenes, abrir visualmente el PDF p.15 con la herramienta Read para verificar que las imagenes page15-93/94/95/96 corresponden a las opciones correctas. El research ya verifico este mapeo, pero Claude debe confirmarlo durante ejecucion:
- page15-95.webp = Sello rojo "ORIGINAL" (Dibujos Exclusivos)
- page15-94.webp = Patron flores tropicales (Estampacion Digital)
- page15-96.webp = Tela roja con marca de agua (Tipo Davos)
- page15-93.webp = Paleta de colores/telas (Desarrollo de Color)

**Paso 3: Crear collars.ts (DATA-02)**

Crear `src/lib/content/collars.ts` con contenido del PDF pp.16-17. Los colores hex son aproximaciones visuales del PDF (decision locked: valores hex aproximados visualmente). Info comercial a discrecion de Claude segun lo relevante del PDF.

```typescript
import type { CollarData } from './types';

export const COLLAR_DATA: CollarData = {
  material: 'Cuellos 100% hilaza poliester Lafayette',
  guarantee: 'Duracion de color, Resistencia, Calidad',
  technologies: ['desempeno', 'proteccion-solar', 'control-humedad'],
  colors: [
    { id: 'negro', name: 'Negro', hex: '#1a1a1a', productCode: '194006' },
    { id: 'blanco', name: 'Blanco', hex: '#FFFFFF', productCode: '110601' },
    { id: 'rojo', name: 'Rojo', hex: '#C42034', productCode: '181663' },
    { id: 'azul', name: 'Azul Oscuro', hex: '#1B3A5C', productCode: '194024' },
  ],
  sizes: {
    children: [
      { size: 'Talla 2 - 4', collarMeasure: '28"7', cuffMeasure: '28"3' },
      { size: 'Talla 6 - 8', collarMeasure: '30"7', cuffMeasure: '28"3' },
      { size: 'Talla 10 - 12', collarMeasure: '32"7', cuffMeasure: '28"3' },
      { size: 'Talla 14 - 16', collarMeasure: '34"7', cuffMeasure: '28"3' },
    ],
    adolescentsAdults: [
      { size: 'Talla XS', collarMeasure: '36"8', cuffMeasure: '37"3' },
      { size: 'Talla S', collarMeasure: '38"8', cuffMeasure: '37"3' },
      { size: 'Talla M', collarMeasure: '40"8', cuffMeasure: '37"3' },
      { size: 'Talla L', collarMeasure: '42"8', cuffMeasure: '37"3' },
      { size: 'Talla XL', collarMeasure: '44"8', cuffMeasure: '37"3' },
      { size: 'Talla 2XL', collarMeasure: '46"8', cuffMeasure: '37"3' },
    ],
  },
  commercialNotes: [
    'Es imprescindible tener un pedido de tela para acompanar el pedido de cuellos.',
    'Otros colores diferentes a los que tienen disponibilidad inmediata, se deben solicitar por programacion.',
    'Ofrecemos el juego de cuellos y punos por programacion.',
  ],
} as const;
```

**IMPORTANTE:** Antes de usar estos datos, abrir visualmente el PDF pp.16-17 con la herramienta Read para verificar:
- Los 4 colores y sus codigos de producto
- Los valores exactos de las tablas de tallas (columnas: talla, medida cuello, medida puno)
- Las notas comerciales

Los colores hex (#C42034 para rojo, #1B3A5C para azul) reutilizan los del brand palette existente (Lafayette rojo/azul oscuro). Si el PDF muestra tonos significativamente diferentes, ajustar.
  </action>
  <verify>
1. `bun run build` no muestra errores de TypeScript
2. Las interfaces PersonalizationOption, CollarColor, CollarSize, CollarData existen en types.ts
3. Technology ahora tiene expandedDescription como campo opcional
4. personalization.ts exporta PERSONALIZATION_OPTIONS con 4 entries
5. collars.ts exporta COLLAR_DATA con 4 colores y 2 grupos de tallas
  </verify>
  <done>types.ts tiene todas las interfaces nuevas + Technology expandida. personalization.ts tiene 4 opciones con datos del PDF p.15. collars.ts tiene datos completos del PDF pp.16-17 (colores, tallas, notas comerciales). Todos los archivos compilan sin errores TypeScript.</done>
</task>

<task type="auto">
  <name>Task 2: Enrich technologies with icons, descriptions and inverse helper</name>
  <files>src/lib/content/technologies.ts, src/lib/content/helpers.ts, src/lib/content/index.ts</files>
  <action>
**DEBT-06: Proveer iconos para 3 tecnologias sin logo**

Las 3 tecnologias con `icon: ''` en technologies.ts necesitan iconos. El CONTEXT.md dice que los iconos EXISTEN en el PDF p.14 y deben extraerse. Sin embargo, el research indica que la extraccion de iconos del PDF puede producir calidad insuficiente (iconos dentro de circulos de color, posible pixelado).

**Procedimiento:**

1. Intentar extraer los iconos de la pagina 14 del PDF usando pdfimages:
   ```bash
   mkdir -p /tmp/tech-icons
   pdfimages -f 14 -l 14 -png Uniformes_Colegios.pdf /tmp/tech-icons/icon
   ```

2. Inspeccionar visualmente los archivos extraidos para identificar los 3 iconos faltantes (algodon, antimanchas, solidez-a-la-luz).

3. **Si los iconos extraidos son usables** (legibles a ~24px, fondo transparente o removible):
   - Copiarlos a `public/images/tech/` con nombres: `algodon.png`, `antimanchas.png`, `solidez-a-la-luz.png`
   - Actualizar los `icon` paths en technologies.ts

4. **Si los iconos NO son usables** (pixelados, con fondo de circulo coloreado ilegible, artefactos):
   - Usar Lucide icon names como fallback (per user decision: "Si la extraccion produce calidad ilegible: usar icono Lucide representativo como fallback")
   - Cambiar el campo `icon` en technologies.ts para las 3 tecnologias:
     - `algodon` -> icon: `'Flower2'` (Lucide icon representando planta de algodon)
     - `antimanchas` -> icon: `'ShieldCheck'` (Lucide icon representando proteccion/escudo)
     - `solidez-a-la-luz` -> icon: `'Sun'` (Lucide icon representando sol/luz)
   - **NOTA:** Si se usan Lucide fallbacks, el formato del campo `icon` cambia de path a nombre de icono. Verificar que los componentes que consumen `technology.icon` (FabricCard tech chips) manejen ambos formatos: paths (`/images/tech/...`) y nombres de Lucide (`'Sun'`, etc.). Si no lo manejan actualmente, agregar un comentario `// TODO Phase 6: handle Lucide icon names for fallback techs` para que Phase 6 lo resuelva al construir los tooltips.

**DATA-03: Expandir descripciones de tecnologias**

Agregar `expandedDescription` a CADA entrada en TECHNOLOGIES en technologies.ts. El contenido DEBE ser texto literal del PDF p.14 (decision locked del usuario: "sin resumir").

Abrir el PDF p.14 con la herramienta Read para obtener el texto exacto. El research indica que el PDF p.14 muestra:
- Titulo general: "Nuestras tecnologias textiles mejoran el desempeno y funcionalidad de la tela, ofreciendo asi innovacion, calidad y beneficios"
- Cada tecnologia tiene su nombre bajo el icono pero NO parrafos descriptivos individuales

Si el PDF no tiene texto expandido por tecnologia, usar el `description` actual como `expandedDescription` (research: "Si el PDF no tiene texto expandido, expandedDescription puede ser undefined o igual a description").

**DATA-03: Agregar relacion inversa getFabricsByTechnology**

Agregar nueva funcion helper en `src/lib/content/helpers.ts`:

```typescript
export function getFabricsByTechnology(techId: string): typeof FABRICS[number][] {
  return FABRICS.filter(f => {
    const techs: readonly string[] = f.technologies;
    return techs.includes(techId);
  });
}
```

Sigue el mismo patron establecido por las 6 helpers existentes (typeof ARRAY[number] return type, readonly string[] cast para includes).

**Actualizar barrel exports en index.ts**

Agregar a `src/lib/content/index.ts`:
```typescript
export { PERSONALIZATION_OPTIONS } from './personalization';
export { COLLAR_DATA } from './collars';
export type { PersonalizationOption, CollarColor, CollarSize, CollarData } from './types';
```

Asegurarse de que getFabricsByTechnology se exporta automaticamente via `export * from './helpers'` (ya existe este re-export).
  </action>
  <verify>
1. `bun run build` pasa sin errores
2. `grep "icon: ''" src/lib/content/technologies.ts` devuelve 0 resultados (todos los iconos tienen valor)
3. `grep 'expandedDescription' src/lib/content/technologies.ts` encuentra entries para cada tecnologia
4. `grep 'getFabricsByTechnology' src/lib/content/helpers.ts` encuentra la funcion
5. `grep 'PERSONALIZATION_OPTIONS' src/lib/content/index.ts` encuentra el export
6. `grep 'COLLAR_DATA' src/lib/content/index.ts` encuentra el export
7. `grep 'CollarData' src/lib/content/index.ts` encuentra el type export
  </verify>
  <done>Las 14 tecnologias tienen iconos (reales o Lucide fallback), expandedDescription poblado, y la funcion getFabricsByTechnology esta disponible. Los nuevos modelos (PERSONALIZATION_OPTIONS, COLLAR_DATA) son importables desde @/lib/content. Build pasa limpio.</done>
</task>

</tasks>

<verification>
1. `bun run build` completa sin errores — todas las paginas SSG generadas
2. `grep "icon: ''" src/lib/content/technologies.ts` devuelve 0 (cero iconos vacios)
3. `grep 'PERSONALIZATION_OPTIONS' src/lib/content/index.ts` confirma export
4. `grep 'COLLAR_DATA' src/lib/content/index.ts` confirma export
5. `grep 'getFabricsByTechnology' src/lib/content/helpers.ts` confirma helper
6. TypeScript compilation: `bunx tsc --noEmit` pasa sin errores
</verification>

<success_criteria>
- Las 3 tecnologias sin icono (algodon, antimanchas, solidez-a-la-luz) tienen icono real extraido del PDF o Lucide fallback
- personalization.ts existe con 4 opciones, datos literales del PDF p.15
- collars.ts existe con 4 colores hex, 2 tablas de tallas, notas comerciales del PDF pp.16-17
- Cada tecnologia tiene expandedDescription
- getFabricsByTechnology helper funciona y se exporta
- Todos los nuevos tipos y datos son importables desde @/lib/content
- `bun run build` pasa limpio
</success_criteria>

<output>
After completion, create `.planning/phases/05-tech-debt-data-foundation/05-02-SUMMARY.md`
</output>
