---
phase: 09-responsive-polish-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/app/usos/page.tsx
  - src/app/uso/[slug]/page.tsx
  - src/app/uso/[slug]/[fabricId]/page.tsx
  - src/app/cuellos/page.tsx
  - src/components/fabric-card.tsx
  - src/components/filterable-fabric-grid.tsx
autonomous: true
requirements:
  - DEPLOY-01
must_haves:
  truths:
    - "La home page se ve correctamente en viewport de 768px sin overflow horizontal ni elementos rotos"
    - "La pagina /usos muestra el grid de categorias legible y navegable en 768px"
    - "Las paginas de categoria /uso/[slug] muestran el grid de telas con spacing adecuado en 768px"
    - "Las fichas tecnicas /uso/[slug]/[fabricId] se leen sin corte de contenido en 768px"
    - "La pagina /cuellos no tiene overflow horizontal en sus tablas en 768px"
    - "Los interactive elements (chips, botones, links) tienen touch targets >= 44px en md"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Home page con md breakpoint polish"
      contains: "md:"
    - path: "src/app/usos/page.tsx"
      provides: "Usos page con md breakpoint polish"
      contains: "md:"
    - path: "src/app/uso/[slug]/page.tsx"
      provides: "Category page con md breakpoint polish"
      contains: "md:"
    - path: "src/components/fabric-card.tsx"
      provides: "FabricCard con touch-friendly sizing en md"
      contains: "md:"
    - path: "src/components/filterable-fabric-grid.tsx"
      provides: "FilterableFabricGrid con touch-friendly chips y controles en md"
      contains: "min-h-"
  key_links:
    - from: "src/components/filterable-fabric-grid.tsx"
      to: "src/components/fabric-card.tsx"
      via: "FabricCard import and rendering"
      pattern: "FabricCard"
    - from: "src/app/uso/[slug]/page.tsx"
      to: "src/components/filterable-fabric-grid.tsx"
      via: "FilterableFabricGrid import and rendering"
      pattern: "FilterableFabricGrid"
---

<objective>
Polish responsive layout para breakpoint md (768px, tablet) en todas las paginas del catalogo.

Purpose: El vendedor debe poder presentar el catalogo profesionalmente en tablet durante reuniones de ventas. Actualmente varias paginas saltan de base a lg sin atencion al rango md (768-1023px).

Output: Todas las paginas del catalogo se ven correctamente, sin overflow horizontal, con spacing adecuado y touch targets >= 44px en viewports de 768px a 1023px.
</objective>

<execution_context>
@/Users/vulture/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vulture/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-responsive-polish-deploy/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Responsive polish para Home y Usos pages (md breakpoint)</name>
  <files>
    src/app/page.tsx
    src/app/usos/page.tsx
  </files>
  <action>
Auditar y ajustar las paginas Home (/) y Usos (/usos) para viewport md (768px):

**Home page (src/app/page.tsx):**
- Hero section: agregar altura intermedia en md. Patron: `h-[320px] md:h-[400px] lg:h-[500px]` (o similar segun el valor actual).
- Grid de 4 navigation cards: evaluar si 2 cols funciona bien en md (probablemente si — 4 items en 2x2 es correcto). Agregar `md:gap-5` si el gap actual salta de base a lg sin intermedio.
- Verificar que el texto del hero (titulo, subtitulo) tiene font-sizes intermedios en md si los actuales saltan de base a lg.
- Asegurar padding horizontal adecuado: `px-4 md:px-6 lg:px-8` si el actual no tiene md.

**Usos page (src/app/usos/page.tsx):**
- Grid de 8 category cards: auditar si tiene md breakpoint intermedio. Patron actual probable: `grid-cols-2 lg:grid-cols-4`. Si 2 cols en md funciona bien (cards de ~360px), solo ajustar gaps: `gap-4 md:gap-5 lg:gap-6`.
- Heading y description: verificar font-sizes y spacing intermedios.

**Principio clave:** Solo agregar `md:` donde mejore la experiencia concreta en tablet. No agregar por completitud. El research confirma que muchos elementos ya funcionan bien con base + lg.

**Touch targets:** Verificar que los links/cards de navegacion tengan area tactil minima >= 44px (probablemente ya cumple por ser cards grandes).
  </action>
  <verify>
Ejecutar `bun run build` para confirmar que no se introducen errores de compilacion. Revisar visualmente los cambios en las clases CSS para confirmar coherencia con los breakpoints existentes.
  </verify>
  <done>
Home page y Usos page tienen clases `md:` donde corresponde para transiciones suaves entre base y lg. No hay overflow horizontal en 768px viewport. Los gaps y font-sizes son intermedios apropiados.
  </done>
</task>

<task type="auto">
  <name>Task 2: Responsive polish para Category pages, fichas tecnicas, y Cuellos (md breakpoint)</name>
  <files>
    src/app/uso/[slug]/page.tsx
    src/app/uso/[slug]/[fabricId]/page.tsx
    src/app/cuellos/page.tsx
  </files>
  <action>
Auditar y ajustar las paginas de categoria, fichas tecnicas y cuellos para viewport md (768px):

**Category pages (/uso/[slug]/page.tsx):**
- El research confirma que CategorySidebar ya funciona en md (pills horizontales con scroll < lg). Mantener.
- El grid de FabricCards: evaluar layout en md. El FilterableFabricGrid usa `grid-cols-2 lg:grid-cols-3`. El research recomienda mantener 2 cols en md (cards de ~370px son adecuadas). Ajustar solo gaps/padding si necesario.
- Container/wrapper: verificar padding `px-4 md:px-6 lg:px-8` si salta de base a lg.
- Heading y breadcrumb: verificar spacing intermedio.

**Fichas tecnicas (/uso/[slug]/[fabricId]/page.tsx):**
- El research indica que la ficha usa `max-w-3xl` (768px max) centrado con layout vertical. En md (768px viewport), el contenido ocupa casi todo el ancho.
- Ajustes menores: verificar padding lateral, font-sizes de specs, y que los tooltips CSS-only no se corten en 768px.
- Si los tooltips se cortan: considerar agregar max-width al tooltip que se adapte en md, o posicionar debajo en lugar de arriba.

**Cuellos (/cuellos/page.tsx):**
- El research indica gap MINOR. Verificar que las tablas de tallas no causan overflow horizontal en 768px.
- Si hay overflow: envolver tablas en `overflow-x-auto` con scroll indicator.
- Grid de colores: actualmente `md:grid-cols-4`. Verificar que funciona sin overflow.
- Ajustar spacing/gaps intermedios si necesario.
  </action>
  <verify>
Ejecutar `bun run build` para confirmar cero errores. Revisar que los archivos modificados tienen clases `md:` coherentes. Verificar que las tablas de cuellos no tienen overflow forzado.
  </verify>
  <done>
Category pages, fichas tecnicas y cuellos se ven correctamente en viewport md. No hay overflow horizontal. Los tooltips no se cortan. Las tablas de tallas son legibles sin scroll horizontal.
  </done>
</task>

<task type="auto">
  <name>Task 3: Touch targets y polish de componentes interactivos para tablet</name>
  <files>
    src/components/fabric-card.tsx
    src/components/filterable-fabric-grid.tsx
  </files>
  <action>
Ajustar componentes interactivos para uso tactil profesional en tablet:

**FabricCard (src/components/fabric-card.tsx):**
- Verificar que el area tactil del card (el Link wrapper) es >= 44px en ambas dimensiones. Probablemente ya cumple por ser un card con imagen + texto.
- Verificar que los technology chips dentro del card tienen padding suficiente para lectura (no necesariamente tocables, son informativos).
- Ajustar image sizes si hay `sizes` prop en next/image que no contempla md: agregar md breakpoint al sizes attribute para que next/image sirva la resolucion correcta.

**FilterableFabricGrid (src/components/filterable-fabric-grid.tsx):**
- **Search input:** Verificar que tiene height >= 44px. Si usa `h-10` (40px), subir a `h-11` (44px) o agregar `min-h-[44px]`.
- **Tech filter chips (botones):** El research identifica que chips con `py-1.5` (~30px total) son insuficientes para touch. Ajustar a `py-2 min-h-[44px]` para cumplir minimo tactil de 44px (Apple HIG / WCAG 2.5.8).
- **Sort select/buttons:** Verificar height >= 44px y agregar min-height si necesario.
- **Clear/reset buttons:** Verificar area tactil suficiente.

**Principio:** Touch targets de 44x44px minimo para TODOS los interactive elements (botones, selects, chips, inputs). Esto es critico para uso tactil en reuniones de ventas.
  </action>
  <verify>
Ejecutar `bun run build` para confirmar cero errores. Revisar que todas las clases de touch target (min-h-[44px], py-2, etc.) estan presentes en los interactive elements.
  </verify>
  <done>
Todos los interactive elements en FabricCard y FilterableFabricGrid tienen touch targets >= 44px. Los filter chips, search input, sort controls y cards son facilmente tocables en tablet.
  </done>
</task>

</tasks>

<verification>
1. `bun run build` completa sin errores ni warnings nuevos
2. Buscar `md:` en los archivos modificados para confirmar que se agregaron breakpoints intermedios
3. Verificar que no hay `min-w-*` fijo ni width fijo que pueda causar overflow en 768px
4. Confirmar que los interactive elements tienen `min-h-[44px]` o equivalente
</verification>

<success_criteria>
- Todas las paginas (home, usos, categorias, fichas tecnicas, cuellos) se ven correctamente en viewport md (768px) sin overflow horizontal
- Touch targets de 44px minimo en todos los interactive elements
- Build exitoso sin errores
- No se rompe el layout existente en lg (desktop)
</success_criteria>

<output>
After completion, create `.planning/phases/09-responsive-polish-deploy/09-01-SUMMARY.md`
</output>
