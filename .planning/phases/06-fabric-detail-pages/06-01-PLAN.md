---
phase: 06-fabric-detail-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/tech-icon.tsx
  - src/components/fabric-card.tsx
  - src/app/uso/[slug]/[fabricId]/page.tsx
autonomous: true
requirements:
  - DETAIL-01
  - DETAIL-02
  - DETAIL-03
  - DETAIL-04
  - DETAIL-05

must_haves:
  truths:
    - "Cada ruta /uso/[slug]/[fabricId] muestra una ficha técnica con nombre, composición, gramaje, ancho, tipo de tejido y base en tabla de 2 columnas"
    - "Las rutas de estampación aparecen como chips separados debajo de la tabla de specs"
    - "Los iconos de tecnología muestran tooltip CSS-only con nombre y descripción al hover (desktop) y tap (tablet)"
    - "Las telas marcadas como nuevas (Apolo, Celta) muestran un badge 'Nuevo' junto al nombre"
    - "Las telas en múltiples categorías muestran links de navegación cruzada a las otras categorías"
    - "Un breadcrumb (Usos > Categoría > Tela) y botón de volver permiten navegación rápida"
    - "NO hay imagen individual por tela — la ficha se centra en datos técnicos (decisión del usuario)"
  artifacts:
    - path: "src/components/tech-icon.tsx"
      provides: "TechIcon reutilizable (imagen path o Lucide name)"
      exports: ["TechIcon"]
    - path: "src/components/fabric-card.tsx"
      provides: "FabricCard actualizado importando TechIcon compartido"
      contains: "import { TechIcon }"
    - path: "src/app/uso/[slug]/[fabricId]/page.tsx"
      provides: "Ficha técnica completa con specs, tooltips, badge, cross-nav"
      contains: "getCategoriesByFabric"
  key_links:
    - from: "src/app/uso/[slug]/[fabricId]/page.tsx"
      to: "src/lib/content/helpers.ts"
      via: "getCategoriesByFabric, getTechnologyById, getCategoryBySlug, getFabricBySlug"
      pattern: "getCategoriesByFabric\\(fabricId\\)"
    - from: "src/app/uso/[slug]/[fabricId]/page.tsx"
      to: "src/components/tech-icon.tsx"
      via: "import TechIcon"
      pattern: "import.*TechIcon.*from.*tech-icon"
    - from: "src/components/fabric-card.tsx"
      to: "src/components/tech-icon.tsx"
      via: "import TechIcon"
      pattern: "import.*TechIcon.*from.*tech-icon"
---

<objective>
Construir la ficha técnica completa de cada tela reemplazando el placeholder actual en `/uso/[slug]/[fabricId]/page.tsx`. La ficha muestra specs en tabla de 2 columnas, chips de rutas de estampación, tecnologías con tooltip CSS-only (hover + tap), badge "Nuevo" condicional, links de navegación cruzada entre categorías, y botón prominente de volver.

Purpose: El vendedor puede mostrar al cliente la información técnica completa de cualquier tela durante la reunión comercial, navegando rápidamente entre telas y categorías.
Output: Ficha técnica funcional como Server Component SSG, TechIcon extraído como componente compartido.
</objective>

<execution_context>
@/Users/vulture/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vulture/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-fabric-detail-pages/06-CONTEXT.md
@.planning/phases/06-fabric-detail-pages/06-RESEARCH.md
@src/app/uso/[slug]/[fabricId]/page.tsx
@src/components/fabric-card.tsx
@src/components/breadcrumb.tsx
@src/lib/content/types.ts
@src/lib/content/helpers.ts
@src/lib/content/index.ts
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract TechIcon to shared component</name>
  <files>
    src/components/tech-icon.tsx
    src/components/fabric-card.tsx
  </files>
  <action>
    Extract the `TechIcon` function from `src/components/fabric-card.tsx` into a new shared component at `src/components/tech-icon.tsx`.

    **Create `src/components/tech-icon.tsx`:**
    - Move the existing TechIcon function as-is (handles dual format: image path starting with `/` renders `<img>`, otherwise resolves Lucide icon name via `icons[name]`)
    - Import `icons` from `lucide-react`
    - Export as named export `TechIcon`
    - Accept props: `{ icon: string; size?: number }` with size defaulting to 14
    - Pass `size` to both `<img>` width/height and `<LucideIcon>` size prop

    **Update `src/components/fabric-card.tsx`:**
    - Remove the internal `TechIcon` function definition
    - Add `import { TechIcon } from '@/components/tech-icon'`
    - Existing usage `<TechIcon icon={tech.icon} />` continues working unchanged

    Do NOT change any other behavior in fabric-card.tsx. The only change is moving TechIcon out and importing it.
  </action>
  <verify>
    Run `bun run build` — zero errors, all 43 fabric detail routes and 8 category pages still generate statically.
  </verify>
  <done>
    TechIcon is a shared component importable from `@/components/tech-icon`. FabricCard imports it and renders identically to before.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build complete fabric detail page with specs, tooltips, badge, and cross-navigation</name>
  <files>
    src/app/uso/[slug]/[fabricId]/page.tsx
  </files>
  <action>
    Rewrite the content of `FabricDetailPage` in `src/app/uso/[slug]/[fabricId]/page.tsx` replacing the placeholder with a complete ficha técnica. Keep `dynamicParams`, `generateStaticParams`, and `generateMetadata` unchanged. The page remains a Server Component (no `'use client'`).

    **Imports to add:**
    - `{ ArrowLeft }` from `lucide-react`
    - `{ TechIcon }` from `@/components/tech-icon`
    - `{ getCategoriesByFabric, getTechnologyById }` to the existing content import
    - Keep existing imports (Metadata, Link, notFound, CATEGORIES, getCategoryBySlug, getFabricBySlug, Breadcrumb)

    **Page layout (single column, centered, max-w-3xl):**
    Per user decision, NO individual fabric image. Layout is a centered single column focused on technical data.

    1. **Breadcrumb** — Already exists, keep as-is: `Usos > {category.name} > {fabric.name}`

    2. **Header section** — `<div className="mt-6 flex items-center gap-3 flex-wrap">`
       - `<h1>` with `text-2xl lg:text-3xl font-heading font-bold text-foreground` showing `fabric.name`
       - Conditional badge: `{fabric.isNew && <span className="rounded-full bg-brand-accent text-brand-accent-foreground px-2.5 py-0.5 text-xs font-semibold">Nuevo</span>}` (DETAIL-04)

    3. **Specs table** (DETAIL-01) — `<table>` with semantic HTML, 2 columns (property | value):
       - Build specs array: `[{ label: 'Composición', value: fabric.composition }, { label: 'Gramaje', value: fabric.weight }, { label: 'Ancho', value: fabric.width }, { label: 'Tipo de Tejido', value: fabric.weave }, { label: 'Base', value: fabric.base }]`
       - Render `<table className="mt-6 w-full text-sm">` with `<tbody>`
       - Each row: `<tr className="border-b border-border">` with `<td className="py-3 pr-4 font-medium text-muted-foreground w-40">{label}</td>` and `<td className="py-3 text-foreground">{value}</td>`

    4. **Print route chips** — Below specs table, `<div className="mt-6">`:
       - Heading: `<h2 className="text-sm font-medium text-muted-foreground mb-3">Rutas de Estampación</h2>`
       - Chips: `<div className="flex flex-wrap gap-2">` with each `fabric.printRoutes.map(route => <span className="rounded-full border border-border bg-surface px-3 py-1 text-xs text-muted-foreground">{route}</span>)`

    5. **Technology chips with CSS-only tooltips** (DETAIL-03) — `<div className="mt-6">`:
       - Heading: `<h2 className="text-sm font-medium text-muted-foreground mb-3">Tecnologías</h2>`
       - Container: `<div className="flex flex-wrap gap-2">`
       - For each `fabric.technologies`, look up `getTechnologyById(techId)`. If found, render:
         ```
         <span className="group relative inline-flex items-center">
           <button type="button" className="inline-flex items-center gap-1.5 rounded-full border border-border bg-muted px-3 py-1 text-sm text-muted-foreground focus:outline-none">
             <TechIcon icon={tech.icon} size={14} />
             {tech.name}
           </button>
           <span role="tooltip" className="pointer-events-none absolute bottom-full left-1/2 -translate-x-1/2 mb-2 w-max max-w-xs rounded-md bg-foreground px-3 py-2 text-xs text-background opacity-0 transition-opacity duration-150 group-hover:opacity-100 group-focus-within:opacity-100 z-10">
             <span className="font-semibold">{tech.name}</span>
             <br />
             {tech.description}
             <span className="absolute top-full left-1/2 -translate-x-1/2 border-4 border-transparent border-t-foreground" />
           </span>
         </span>
         ```
       - CRITICAL: Use `group-hover:opacity-100 group-focus-within:opacity-100` together for desktop hover AND tablet tap support. Tailwind v4 wraps hover in `@media (hover: hover)` which excludes tablets. The `<button>` element receives focus on tap, triggering `group-focus-within`.
       - Accessibility: `role="tooltip"` on tooltip span, `aria-describedby` linking trigger to tooltip via unique `id={`tooltip-${tech.id}`}`

    6. **Cross-navigation** (DETAIL-05):
       - Compute: `const otherCategories = getCategoriesByFabric(fabricId).filter(c => c.id !== slug)`
       - Only render if `otherCategories.length > 0`
       - Section: `<div className="mt-8 rounded-lg border border-border bg-surface p-4">`
       - Heading: `<h2 className="text-sm font-medium text-muted-foreground mb-3">También disponible en</h2>`
       - Links: `<div className="flex flex-wrap gap-2">` with each `otherCategories.map(cat => <Link href={`/uso/${cat.id}/${fabricId}`} className="inline-flex items-center rounded-full px-3 py-1.5 text-sm border border-border hover:bg-muted transition-colors">{cat.name}</Link>)`
       - Link destination is `/uso/{otherCategorySlug}/{fabricId}` (same fabric in other category context — most useful for vendor showing the same fabric works for different uses)

    7. **Back button** — Prominent at the bottom:
       ```
       <Link href={`/uso/${slug}`} className="mt-8 inline-flex items-center gap-2 rounded-lg bg-muted px-4 py-2.5 text-sm font-medium text-foreground hover:bg-border transition-colors">
         <ArrowLeft size={16} />
         Volver a {category.name}
       </Link>
       ```

    **Outer wrapper:** Change `max-w-7xl` to `max-w-3xl mx-auto` for centered narrow layout (per research recommendation — no image means no second column to justify wide layout).

    **DETAIL-02 compliance:** The user explicitly decided NOT to include an individual fabric image. The generic category images repeat the same photo for all fabrics in a category and add no value. This requirement is satisfied by the explicit user override documented in 06-CONTEXT.md. Do NOT include any `<Image>` or `<img>` tag for the fabric in the detail page.
  </action>
  <verify>
    1. Run `bun run build` — zero errors, all 43 fabric detail routes generate statically
    2. Run `bun run dev` and verify:
       - `/uso/sudaderas/vendaval-crush` shows specs table with composition, gramaje, ancho, tejido, base
       - Print route chips visible below table
       - Technology chips show tooltip on hover
       - `/uso/sudaderas/apolo` shows "Nuevo" badge (isNew: true)
       - `/uso/sudaderas/orion-clororresistente` shows "También disponible en" with links to chaquetas-prom and delantales
       - `/uso/sudaderas/vendaval-crush` does NOT show cross-nav (single category only)
       - Back button links to `/uso/sudaderas`
       - No `'use client'` directive — pure Server Component
  </verify>
  <done>
    Every fabric detail page at `/uso/[slug]/[fabricId]` shows a complete ficha técnica with: (1) breadcrumb, (2) name + optional badge "Nuevo", (3) specs table 2 columnas, (4) print route chips, (5) technology chips with CSS-only tooltips, (6) cross-navigation links when multi-category, (7) back button. No individual fabric image per user decision.
  </done>
</task>

</tasks>

<verification>
1. `bun run build` completes with zero errors — all 43 fabric detail pages + 8 category pages generate as static
2. TechIcon is exported from `src/components/tech-icon.tsx` and imported by both `fabric-card.tsx` and `page.tsx`
3. No `'use client'` directive in the detail page — pure Server Component
4. Tooltip uses `group-hover` + `group-focus-within` (not hover alone) for tablet compatibility
5. Cross-navigation correctly filters out current category from links
6. Badge "Nuevo" appears on Apolo and Celta fabric pages only
7. No `<Image>` or `<img>` for individual fabric (user decision honored)
</verification>

<success_criteria>
- All 43 fabric detail routes render complete fichas técnicas with specs, tooltips, badge, and cross-nav
- Build succeeds with zero errors and all routes remain SSG static
- TechIcon is shared between FabricCard and the detail page without duplication
- Tooltips work via CSS-only (group-hover + group-focus-within) — no JavaScript tooltip library
</success_criteria>

<output>
After completion, create `.planning/phases/06-fabric-detail-pages/06-01-SUMMARY.md`
</output>
