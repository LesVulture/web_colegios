---
phase: 04-category-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/breadcrumb.tsx
  - src/components/category-header.tsx
  - src/components/category-sidebar.tsx
  - src/components/skeleton-card.tsx
  - src/components/fabric-card.tsx
autonomous: true
requirements: [CAT-02, CAT-03, CAT-04]

must_haves:
  truths:
    - "FabricCard renders fabric name, image, and technology chips as text badges"
    - "CategoryHeader displays category name with its distinctive background color and foreground text"
    - "CategorySidebar lists all 8 categories as navigation links"
    - "Breadcrumb shows navigation path like 'Usos > Sudaderas'"
    - "SkeletonCard mimics the FabricCard shape with shimmer animation"
  artifacts:
    - path: "src/components/fabric-card.tsx"
      provides: "Product card with image, name, tech chips, hover elevation, link to fabric detail"
      contains: "FabricCard"
    - path: "src/components/category-header.tsx"
      provides: "Category header with color background, name, description, fabric count"
      contains: "CategoryHeader"
    - path: "src/components/category-sidebar.tsx"
      provides: "Sidebar navigation listing 8 categories with active state"
      contains: "CategorySidebar"
    - path: "src/components/breadcrumb.tsx"
      provides: "Reusable breadcrumb with chevron separators"
      contains: "Breadcrumb"
    - path: "src/components/skeleton-card.tsx"
      provides: "Skeleton card with shimmer animation matching FabricCard shape"
      contains: "SkeletonCard"
    - path: "src/app/globals.css"
      provides: "Shimmer keyframe animation"
      contains: "@keyframes shimmer"
  key_links:
    - from: "src/components/fabric-card.tsx"
      to: "@/lib/content"
      via: "getTechnologyById import"
      pattern: "getTechnologyById"
    - from: "src/components/category-header.tsx"
      to: "@/lib/content/styles"
      via: "CATEGORY_STYLE_MAP import"
      pattern: "CATEGORY_STYLE_MAP"
    - from: "src/components/category-sidebar.tsx"
      to: "@/lib/content"
      via: "CATEGORIES import"
      pattern: "CATEGORIES"
    - from: "src/components/fabric-card.tsx"
      to: "/uso/[slug]/[fabricId]"
      via: "Link href"
      pattern: "uso.*categorySlug.*fabric.id"
---

<objective>
Create all reusable UI components for the category pages: FabricCard (product card with tech chips and hover elevation), CategoryHeader (color-coded header with category info), CategorySidebar (8-category navigation with active state), Breadcrumb (path navigation), and SkeletonCard (shimmer loading placeholder). Also add the shimmer CSS keyframe to globals.css.

Purpose: These components are the building blocks that the category page route (Plan 02) will compose. Extracting them as reusable components enables consistent UI across all 8 category pages and future reuse in Phase 5 (fabric details).

Output: 5 component files in src/components/ + updated globals.css with shimmer animation.
</objective>

<execution_context>
@/Users/vulture/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vulture/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-category-pages/04-CONTEXT.md
@.planning/phases/04-category-pages/04-RESEARCH.md
@src/lib/content/types.ts
@src/lib/content/styles.ts
@src/lib/content/helpers.ts
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shimmer CSS and create Breadcrumb, CategoryHeader, and SkeletonCard components</name>
  <files>
    src/app/globals.css
    src/components/breadcrumb.tsx
    src/components/category-header.tsx
    src/components/skeleton-card.tsx
  </files>
  <action>
**globals.css:** Add the shimmer keyframe animation AFTER the existing `@layer base` block. Add:

```css
/* --- Shimmer skeleton animation --- */
@keyframes shimmer {
  0% { background-position: -200% 0; }
  100% { background-position: 200% 0; }
}

.skeleton-shimmer {
  background: linear-gradient(90deg, var(--color-muted) 25%, var(--color-border) 50%, var(--color-muted) 75%);
  background-size: 200% 100%;
  animation: shimmer 1.5s infinite;
}
```

**breadcrumb.tsx:** Create a reusable Breadcrumb Server Component. Props: `items: { label: string; href?: string }[]`. Render as a `<nav aria-label="Breadcrumb">` with flex row, `text-sm text-muted-foreground`. Use `ChevronRight` from `lucide-react` (size 14) as separator between items. Last item (no href) renders as `<span className="text-foreground font-medium">`, other items as `<Link>` with `hover:text-foreground transition-colors`.

**category-header.tsx:** Create a CategoryHeader Server Component. Props: `category: Category` (from `@/lib/content/types`), `fabricCount: number`. Import `CATEGORY_STYLE_MAP` from `@/lib/content/styles`. Render a `<div>` with `${colors.bg} ${colors.fg} rounded-lg p-6 lg:p-8`. Include:
- `<h1>` with `text-2xl lg:text-3xl font-heading font-bold` showing `category.name`
- Conditional description: use `'description' in category` operator (same pattern as usos page) with `opacity-80 text-sm mt-1`
- Fabric count: `<p className="mt-2 text-sm opacity-70">{fabricCount} telas disponibles</p>`

**skeleton-card.tsx:** Create a SkeletonCard Server Component. No props. Mimics the FabricCard layout:
- Outer div: `rounded-lg border border-border overflow-hidden`
- Image area: `<div className="aspect-[4/3] skeleton-shimmer" />`
- Content area: `<div className="p-4 space-y-2">` with:
  - Title placeholder: `<div className="h-5 w-3/4 rounded skeleton-shimmer" />`
  - Chips placeholder: `<div className="flex gap-1.5">` with 2 pill-shaped divs `<div className="h-5 w-16 rounded-full skeleton-shimmer" />` and `<div className="h-5 w-20 rounded-full skeleton-shimmer" />`
  </action>
  <verify>Run `bunx tsc --noEmit` to confirm no TypeScript errors. Verify all 4 files exist and contain their expected exports.</verify>
  <done>globals.css has shimmer keyframe and .skeleton-shimmer class. Breadcrumb, CategoryHeader, and SkeletonCard components compile without errors and match the specified structure.</done>
</task>

<task type="auto">
  <name>Task 2: Create CategorySidebar and FabricCard components</name>
  <files>
    src/components/category-sidebar.tsx
    src/components/fabric-card.tsx
  </files>
  <action>
**category-sidebar.tsx:** Create a Client Component (`'use client'`). No props needed. Import: `Link` from `next/link`, `usePathname` from `next/navigation`, `CATEGORIES` from `@/lib/content`, `CATEGORY_STYLE_MAP` from `@/lib/content/styles`, `cn` from `@/lib/utils`. Extract current slug from pathname: `const currentSlug = pathname.split('/')[2]` (splits `/uso/[slug]`).

Render as `<nav className="space-y-1">`:
- Map over CATEGORIES. Each item is a `<Link href={/uso/${cat.id}}>`
- Active state: if `cat.id === currentSlug`, apply `${colors.bg} ${colors.fg}` classes
- Inactive state: `text-foreground/70 hover:bg-muted`
- Common classes: `block rounded-md px-3 py-2 text-sm font-medium transition-colors`
- Use `cn()` for conditional class merging

For tablet (md to lg), this sidebar should become a horizontal scrollable bar. Add a wrapper: `<aside className="hidden lg:block lg:w-56 lg:shrink-0">` for desktop sidebar, and a separate `<div className="lg:hidden mb-6 overflow-x-auto">` with a flex row of compact pill-shaped links for tablet. Both render the same category links but with different layouts.

**fabric-card.tsx:** Create a Server Component. Props: `fabric: Fabric` (from types), `categorySlug: string`. Import: `Image` from `next/image`, `Link` from `next/link`, `getTechnologyById` from `@/lib/content`.

Structure:
- `<Link href={/uso/${categorySlug}/${fabric.id}}>`
- Classes: `group block rounded-lg border border-border bg-background overflow-hidden transition-all duration-200 hover:shadow-lg hover:-translate-y-1` (per locked decision: subtle shadow + elevation on hover)
- Image container: `<div className="relative aspect-[4/3]">` with `<Image src={fabric.image} alt={fabric.name} fill sizes="(min-width: 1024px) 33vw, 50vw" className="object-cover" />`
- Content: `<div className="p-4">`
  - Name: `<h3 className="font-heading font-semibold text-foreground">{fabric.name}</h3>`
  - Tech chips (per locked decision: badges with visible text, not just icons): `<div className="mt-2 flex flex-wrap gap-1.5">` mapping over `fabric.technologies`, calling `getTechnologyById(techId)`, rendering each as `<span className="inline-block rounded-full border border-border bg-muted px-2.5 py-0.5 text-xs text-muted-foreground">{tech.name}</span>`
  </action>
  <verify>Run `bunx tsc --noEmit` to confirm no TypeScript errors. Run `bun run build` to verify no build errors. Check that FabricCard links to `/uso/[categorySlug]/[fabric.id]` and CategorySidebar uses `usePathname()` for active state detection.</verify>
  <done>CategorySidebar renders 8 category links with active highlighting (desktop sidebar + tablet horizontal scroll). FabricCard renders fabric name, image, and technology text chips with hover elevation effect and link to fabric detail route.</done>
</task>

</tasks>

<verification>
1. `bunx tsc --noEmit` passes (all 6 files compile)
2. `bun run build` succeeds (no build errors)
3. All 5 component files exist in src/components/
4. globals.css contains `@keyframes shimmer` and `.skeleton-shimmer`
5. FabricCard links to `/uso/{categorySlug}/{fabricId}` (not `/tela/...`)
6. CategorySidebar is a Client Component using usePathname()
7. CategoryHeader uses CATEGORY_STYLE_MAP (not dynamic class interpolation)
8. Technology chips show text names (not just icons) per locked decision
</verification>

<success_criteria>
All 5 reusable components created and TypeScript-clean. Shimmer CSS animation added to globals.css. Components follow established project patterns (cn() utility, CATEGORY_STYLE_MAP, as-const-satisfies data access, preload not priority for images).
</success_criteria>

<output>
After completion, create `.planning/phases/04-category-pages/04-01-SUMMARY.md`
</output>
