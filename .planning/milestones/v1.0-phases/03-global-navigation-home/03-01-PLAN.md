---
phase: 03-global-navigation-home
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/content/styles.ts
  - src/lib/nav.ts
  - src/components/header.tsx
  - src/components/nav-links.tsx
  - src/components/mobile-menu.tsx
  - src/app/layout.tsx
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03]

must_haves:
  truths:
    - "El logo Lafayette es visible en la esquina superior izquierda de todas las páginas con 12px offset del borde superior"
    - "El menú de navegación muestra exactamente 4 items: Usos, Tecnologías, Personalización, Cuellos con texto e iconos"
    - "En tablet (< lg), el menú se reemplaza por un hamburger que abre un sidebar deslizante desde la derecha"
    - "El header es sticky y persiste entre navegaciones de página"
  artifacts:
    - path: "src/lib/content/styles.ts"
      provides: "CATEGORY_STYLE_MAP compartido para color de categoría → clases Tailwind"
      contains: "CATEGORY_STYLE_MAP"
    - path: "src/lib/nav.ts"
      provides: "NAV_ITEMS constante centralizada con hrefs, labels e iconos"
      contains: "NAV_ITEMS"
    - path: "src/components/header.tsx"
      provides: "Header Server Component wrapper con logo y navegación"
      contains: "Header"
    - path: "src/components/nav-links.tsx"
      provides: "Client Component con usePathname para active link state"
      contains: "NavLinks"
    - path: "src/components/mobile-menu.tsx"
      provides: "Client Component con hamburger toggle y sidebar overlay"
      contains: "MobileMenu"
    - path: "src/app/layout.tsx"
      provides: "Root layout con Header integrado"
      contains: "<Header />"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/header.tsx"
      via: "import and render"
      pattern: "import.*Header.*from.*components/header"
    - from: "src/components/header.tsx"
      to: "src/components/nav-links.tsx"
      via: "composition (desktop nav)"
      pattern: "<NavLinks"
    - from: "src/components/header.tsx"
      to: "src/components/mobile-menu.tsx"
      via: "composition (tablet hamburger)"
      pattern: "<MobileMenu"
    - from: "src/components/nav-links.tsx"
      to: "src/lib/nav.ts"
      via: "import NAV_ITEMS"
      pattern: "import.*NAV_ITEMS.*from.*lib/nav"
---

<objective>
Crear el header global con logo Lafayette, navegación de 4 items con iconos, y menú hamburger responsivo para tablet. Instalar lucide-react como dependencia de iconos.

Purpose: Establecer la navegación principal que aparece en todas las páginas del sitio, permitiendo al vendedor moverse fluidamente entre secciones durante la reunión.
Output: Header funcional en root layout, visible en todas las rutas, con navegación desktop y mobile.
</objective>

<execution_context>
@/Users/vulture/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vulture/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-global-navigation-home/03-RESEARCH.md
@src/app/layout.tsx
@src/app/globals.css
@src/lib/utils.ts
@src/lib/content/categories.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar lucide-react, crear constantes compartidas (NAV_ITEMS, CATEGORY_STYLE_MAP)</name>
  <files>
    src/lib/nav.ts
    src/lib/content/styles.ts
  </files>
  <action>
1. Instalar lucide-react: `bun add lucide-react`

2. Crear `src/lib/nav.ts` con un array constante `NAV_ITEMS` que define los 4 items de navegación:
   - `{ href: '/usos', label: 'Usos', icon: 'LayoutGrid' }`
   - `{ href: '/tecnologias', label: 'Tecnologías', icon: 'Cpu' }`
   - `{ href: '/personalizacion', label: 'Personalización', icon: 'Palette' }`
   - `{ href: '/cuellos', label: 'Cuellos', icon: 'Shirt' }`

   El campo `icon` es el nombre del icono de lucide-react (string). Exportar también el tipo `NavItem`. Esto centraliza las rutas para evitar inconsistencias entre header y home grid (pitfall 6 de RESEARCH.md).

3. Crear `src/lib/content/styles.ts` moviendo el `categoryColorMap` que hoy vive en `src/app/page.tsx` a una constante compartida `CATEGORY_STYLE_MAP`:
   ```typescript
   export const CATEGORY_STYLE_MAP: Record<string, { bg: string; fg: string }> = {
     'sudaderas-chaquetas-pantalones': { bg: 'bg-cat-sudaderas', fg: 'text-cat-sudaderas-fg' },
     'camisetas-polos': { bg: 'bg-cat-camisetas', fg: 'text-cat-camisetas-fg' },
     'uniforme-deportivo': { bg: 'bg-cat-deportivo', fg: 'text-cat-deportivo-fg' },
     'uniforme-diario-faldas-blazers': { bg: 'bg-cat-diario', fg: 'text-cat-diario-fg' },
     'buzos-hoodies-perchados': { bg: 'bg-cat-buzos', fg: 'text-cat-buzos-fg' },
     'chaquetas-prom': { bg: 'bg-cat-chaquetas-prom', fg: 'text-cat-chaquetas-prom-fg' },
     'blusas-camisas': { bg: 'bg-cat-blusas', fg: 'text-cat-blusas-fg' },
     'delantales-batas-laboratorio': { bg: 'bg-cat-delantales', fg: 'text-cat-delantales-fg' },
   } as const
   ```
   NOTA: Las keys deben coincidir con los `id` de CATEGORIES en `categories.ts` (son los slugs completos, no los slugs cortos del page.tsx actual).
  </action>
  <verify>
    `bun run build` compila sin errores. Los imports `import { NAV_ITEMS } from '@/lib/nav'` y `import { CATEGORY_STYLE_MAP } from '@/lib/content/styles'` resuelven correctamente.
  </verify>
  <done>
    lucide-react instalada en package.json. NAV_ITEMS exporta 4 items con href/label/icon. CATEGORY_STYLE_MAP mapea los 8 IDs de categoría a clases Tailwind bg/fg.
  </done>
</task>

<task type="auto">
  <name>Task 2: Crear componentes de Header, NavLinks y MobileMenu, integrar en root layout</name>
  <files>
    src/components/header.tsx
    src/components/nav-links.tsx
    src/components/mobile-menu.tsx
    src/app/layout.tsx
  </files>
  <action>
1. Crear `src/components/` directory.

2. Crear `src/components/header.tsx` como Server Component (sin 'use client'):
   - Sticky header: `sticky top-0 z-40`
   - Fondo semi-transparente con blur: `bg-background/95 backdrop-blur-sm` (per discretion recommendation: contexto de reunión requiere acceso rápido al menú)
   - Padding top `pt-3` (12px) para cumplir NAV-01 offset del borde superior
   - Contiene: logo Lafayette a la izquierda (next/image, `public/images/logo-lafayette.png`, width ~140-160px auto height), NavLinks a la derecha (hidden en < lg), MobileMenu (visible solo en < lg)
   - Layout: `flex items-center justify-between` dentro de un container `mx-auto max-w-7xl px-4 lg:px-8`
   - Borde inferior sutil: `border-b border-border/50`

3. Crear `src/components/nav-links.tsx` como Client Component (`'use client'`):
   - Import `usePathname` de `next/navigation`
   - Import `NAV_ITEMS` de `@/lib/nav`
   - Import iconos por nombre de `lucide-react`: `{ LayoutGrid, Cpu, Palette, Shirt }`
   - Crear un mapa `iconMap` local que resuelve el string de NAV_ITEMS al componente: `{ LayoutGrid, Cpu, Palette, Shirt }`
   - Renderizar un `<nav>` con `flex items-center gap-6`
   - Cada item es un `<Link>` con icono (size={18}) + label
   - Active state: si `pathname.startsWith(item.href)`, aplicar `text-brand-accent font-semibold` via `cn()`. Sino: `text-foreground/70 hover:text-foreground transition-colors`
   - Recibir prop opcional `onNavigate?: () => void` para que MobileMenu pueda cerrarse al navegar. Llamar `onNavigate?.()` en el onClick de cada Link.
   - Cuando se use en modo vertical (mobile), aceptar prop `orientation?: 'horizontal' | 'vertical'` que cambia layout a `flex-col gap-4` y hace items más grandes (`text-lg gap-3 py-2`)

4. Crear `src/components/mobile-menu.tsx` como Client Component (`'use client'`):
   - Import `useState, useEffect` de 'react'
   - Import `usePathname` de `next/navigation`
   - Import `Menu, X` de `lucide-react`
   - `useState<boolean>(false)` para isOpen
   - `useEffect` que escucha cambios de pathname y cierra el menú (pitfall 4 de RESEARCH.md)
   - Botón hamburger: `lg:hidden p-2` con `aria-label` y `aria-expanded`
   - Panel sidebar: `fixed inset-0 z-50 lg:hidden` cuando isOpen
     - Overlay: `absolute inset-0 bg-black/50` con onClick para cerrar
     - Nav panel: `absolute right-0 top-0 h-full w-72 bg-background p-6 shadow-xl` con `transition-transform duration-300` (sidebar derecha per discretion recommendation)
     - Dentro del panel: logo Lafayette arriba, luego `<NavLinks orientation="vertical" onNavigate={() => setIsOpen(false)} />`
   - Todo el componente `lg:hidden` (solo visible en tablet)

5. Actualizar `src/app/layout.tsx`:
   - Agregar `import { Header } from '@/components/header'`
   - Dentro del `<body>`, insertar `<Header />` antes del `<main>{children}</main>`
   - Agregar `className="flex min-h-screen flex-col"` al body
   - El `<main>` debe tener `className="flex-1"` para push footer pattern
  </action>
  <verify>
    `bun run build` compila sin errores. `bun run dev` muestra el header en la página actual (design system preview). El logo se ve en la esquina superior izquierda. Los 4 items de navegación son visibles en desktop. Al reducir el viewport a < 1024px, aparece el hamburger y los nav items se ocultan.
  </verify>
  <done>
    Header sticky con logo Lafayette (12px offset top), 4 nav items con iconos lucide-react en desktop, hamburger menu con sidebar slide-in en tablet. Integrado en root layout, visible en todas las páginas. Navigation links centralizados en NAV_ITEMS.
  </done>
</task>

</tasks>

<verification>
1. `bun run build` compila sin errores TypeScript
2. El header aparece en todas las páginas (verificar en / y futuras rutas)
3. Logo Lafayette visible arriba-izquierda con ~12px de espacio al borde superior
4. 4 nav items visibles en desktop (>= 1024px) con iconos y texto
5. Hamburger menu funcional en tablet (< 1024px) con sidebar deslizante
6. El menú mobile se cierra al navegar a otra página
7. Active link state funciona (item actual resaltado en color accent)
</verification>

<success_criteria>
- Header global con logo y 4 nav items renderiza en layout.tsx
- lucide-react instalada y funcionando con iconos LayoutGrid, Cpu, Palette, Shirt
- Responsive: desktop muestra nav bar, tablet muestra hamburger con sidebar
- NAV_ITEMS centralizado evita rutas inconsistentes
- CATEGORY_STYLE_MAP extraído para reutilización en Plans 02 y 03
</success_criteria>

<output>
After completion, create `.planning/phases/03-global-navigation-home/03-01-SUMMARY.md`
</output>
