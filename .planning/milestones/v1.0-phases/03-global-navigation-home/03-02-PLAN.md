---
phase: 03-global-navigation-home
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/app/page.tsx
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03]

must_haves:
  truths:
    - "La home page muestra un hero con foto grande de uniformes y branding 'Lafayette Uni For Me Colegios' superpuesto"
    - "El hero NO tiene botón ni call-to-action, solo branding visual"
    - "Debajo del hero hay un grid de 4 cards (Usos, Tecnologías, Personalización, Cuellos) con imagen de fondo representativa"
    - "El grid es de 4 columnas en desktop y 2 columnas en tablet"
    - "Cada card enlaza a su sección correspondiente (/usos, /tecnologias, /personalizacion, /cuellos)"
    - "Los 4 items del grid son la ÚNICA navegación de contenido visible en la home (no hay sidebar ni secciones extra)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Home page con hero section y grid de 4 secciones principales"
      min_lines: 50
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/nav.ts"
      via: "import NAV_ITEMS para grid de 4 secciones"
      pattern: "import.*NAV_ITEMS.*from.*lib/nav"
    - from: "src/app/page.tsx grid cards"
      to: "/usos, /tecnologias, /personalizacion, /cuellos"
      via: "next/link href matching NAV_ITEMS"
      pattern: "<Link.*href=.*/(usos|tecnologias|personalizacion|cuellos)"
---

<objective>
Reemplazar la página de design system preview actual con la home page real: hero con branding Lafayette y grid de 4 secciones principales.

Purpose: Dar al vendedor un punto de entrada profesional e impactante al catálogo, con acceso directo a las 4 secciones principales.
Output: Home page funcional con hero visual y navegación principal por cards.
</objective>

<execution_context>
@/Users/vulture/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vulture/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-global-navigation-home/03-RESEARCH.md
@.planning/phases/03-global-navigation-home/03-01-SUMMARY.md
@src/lib/nav.ts
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear home page con hero section y grid de 4 secciones</name>
  <files>
    src/app/page.tsx
  </files>
  <action>
Reemplazar completamente el contenido actual de `src/app/page.tsx` (design system preview) con la home page real.

**Hero Section:**
- Contenedor `relative` con height `h-[400px] lg:h-[500px]` y `overflow-hidden rounded-lg` (usar rounded-lg = 1rem per design system)
- Imagen de fondo: `next/image` con `fill` prop, `sizes="100vw"`, `className="object-cover"`, y `preload` (NO usar `priority` — deprecated en Next.js 16, usar `preload` per RESEARCH.md State of the Art)
- Usar imagen de uniformes escolares del PDF: `/images/content/page13-36.webp` (foto de grupo con uniformes, la más representativa). El implementador debe revisar las imágenes disponibles y elegir la mejor candidata; si page13-36 no es adecuada, revisar page13-38, page13-39, page13-41 como alternativas.
- Overlay semi-transparente sobre la imagen: `absolute inset-0 bg-brand-primary/60`
- Texto superpuesto centrado vertical y horizontalmente: `relative z-10 flex h-full items-center justify-center`
  - "Lafayette Uni For Me" en línea 1
  - "Colegios" en línea 2
  - Tipografía heading (Raleway) grande: `text-4xl lg:text-6xl font-heading font-bold text-white text-center`
  - Puede incluir un subtítulo sutil debajo: "Soluciones textiles para uniformes escolares" en `text-lg text-white/80`
- SIN botón ni call-to-action (decisión locked del usuario)

**Grid de 4 Secciones:**
- Debajo del hero, con margen superior `mt-8 lg:mt-12`
- Import `NAV_ITEMS` de `@/lib/nav` para obtener los 4 items (mismos que el header — fuente única de verdad)
- Grid `grid grid-cols-2 lg:grid-cols-4 gap-4 lg:gap-6`
- Cada item es un `<Link>` de next/link apuntando a `item.href`
- Cada card tiene:
  - Contenedor `relative overflow-hidden rounded-lg` con height fijo `h-48 lg:h-56`
  - Imagen de fondo representativa de la sección (usar `next/image` con `fill`):
    - Usos: usar una imagen de contenido genérica de uniformes (ej: `/images/content/page13-38.webp` o similar)
    - Tecnologías: usar `/images/content/page15-85.webp` o similar
    - Personalización: usar `/images/content/page16-103.webp` o similar
    - Cuellos: usar `/images/content/page18-107.webp` o similar
    El implementador DEBE revisar las imágenes disponibles en `public/images/content/` y asignar la más representativa para cada sección. Si no hay imagen adecuada para alguna sección, usar un fondo sólido con el color de marca como fallback (`bg-brand-primary` o `bg-brand-accent`).
  - Overlay gradiente sutil para legibilidad del texto: `absolute inset-0 bg-gradient-to-t from-black/70 via-black/30 to-transparent`
  - Texto en la parte inferior: icono de lucide-react (size={24}) + label, en `absolute bottom-0 left-0 right-0 p-4 text-white`
  - `cursor-pointer` y hover effect: `transition-transform duration-300 hover:scale-[1.02]` (per ui-ux-pro-max skill: cursor-pointer en clickables, hover feedback)
- Los 4 items son la ÚNICA navegación visible (HOME-03). No agregar sidebar, footer de links, ni secciones adicionales.

**Layout general:**
- Container principal: `mx-auto max-w-7xl px-4 lg:px-8 py-8 lg:py-12`
- Solo hero + grid. Nada más. Clean y directo (per decisión: densidad de contenido punto medio).

**Importaciones necesarias:**
- `Image` de `next/image`
- `Link` de `next/link`
- `NAV_ITEMS` de `@/lib/nav`
- Iconos de lucide-react: `{ LayoutGrid, Cpu, Palette, Shirt }` (mismos del nav)
- Crear un mapa local `iconMap` para resolver string de icon a componente (mismo patrón que nav-links.tsx)
  </action>
  <verify>
    `bun run build` compila sin errores. `bun run dev` muestra la home con hero + grid de 4 items. Los 4 links navegan correctamente a /usos, /tecnologias, /personalizacion, /cuellos (pueden dar 404 si las páginas aún no existen, pero los hrefs deben ser correctos). No hay botón en el hero. El grid es 4 columnas en desktop y 2 en tablet.
  </verify>
  <done>
    Home page reemplaza el design system preview. Hero con foto de uniformes + branding "Lafayette Uni For Me Colegios" sin CTA. Grid de 4 cards con imagen de fondo enlazando a las 4 secciones principales. Layout limpio sin elementos extra. Responsive 4col desktop / 2col tablet.
  </done>
</task>

</tasks>

<verification>
1. `bun run build` compila sin errores
2. La home ya NO muestra el design system preview
3. Hero visible con imagen de fondo, overlay y branding centrado
4. Hero sin botón ni call-to-action
5. Grid de 4 items debajo del hero con imágenes o fallback de color
6. Grid: 4 columnas en desktop (>= 1024px), 2 columnas en tablet
7. Cada card enlaza a la ruta correcta (/usos, /tecnologias, /personalizacion, /cuellos)
8. Los hrefs coinciden con NAV_ITEMS (fuente única de verdad)
</verification>

<success_criteria>
- Home page profesional con hero visual impactante y branding Lafayette
- Grid de 4 secciones como única navegación de contenido
- Responsive grid (4 col desktop, 2 col tablet)
- Rutas consistentes con NAV_ITEMS
- Estética vibrante y colorida per decisiones del usuario
</success_criteria>

<output>
After completion, create `.planning/phases/03-global-navigation-home/03-02-SUMMARY.md`
</output>
