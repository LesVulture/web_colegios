---
phase: 07-content-section-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/tecnologias/page.tsx
autonomous: true
requirements: [SECTION-01]

must_haves:
  truths:
    - "La pagina /tecnologias muestra 14 cards de tecnologias con icono (PNG o Lucide fallback), nombre y descripcion"
    - "Cada card de tecnologia lista las telas que la usan como chips con links navegables a fichas de detalle (/uso/{cat}/{fabricId})"
    - "Las cards tienen hover effects (sombra, elevacion) y animacion de entrada staggered"
    - "Breadcrumb visible: Inicio > Tecnologias"
  artifacts:
    - path: "src/app/globals.css"
      provides: "CSS keyframe animation fade-in-up for staggered card reveals"
      contains: "fade-in-up"
    - path: "src/app/tecnologias/page.tsx"
      provides: "Complete Tecnologias page with 14 tech cards and cross-navigation"
      min_lines: 60
  key_links:
    - from: "src/app/tecnologias/page.tsx"
      to: "src/lib/content/technologies.ts"
      via: "TECHNOLOGIES import"
      pattern: "import.*TECHNOLOGIES.*from.*@/lib/content"
    - from: "src/app/tecnologias/page.tsx"
      to: "src/lib/content/helpers.ts"
      via: "getFabricsByTechnology and getCategoriesByFabric for cross-navigation"
      pattern: "getFabricsByTechnology|getCategoriesByFabric"
    - from: "src/app/tecnologias/page.tsx"
      to: "src/components/tech-icon.tsx"
      via: "TechIcon component for dual icon format"
      pattern: "import.*TechIcon.*from.*@/components/tech-icon"
    - from: "src/app/tecnologias/page.tsx"
      to: "src/components/breadcrumb.tsx"
      via: "Breadcrumb component"
      pattern: "import.*Breadcrumb.*from.*@/components/breadcrumb"
---

<objective>
Build the Tecnologias Textiles page with a visually impressive grid of 14 technology cards featuring icons, descriptions, cross-navigation links to associated fabrics, and CSS entry animations.

Purpose: Replace the placeholder /tecnologias page with a production-quality page that the salesperson can use during B2B meetings to showcase Lafayette's textile technologies and their associated fabrics.
Output: Complete /tecnologias page with animated card grid and cross-navigation to fabric detail pages.
</objective>

<execution_context>
@/Users/vulture/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vulture/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-content-section-pages/07-RESEARCH.md
@.planning/phases/05-tech-debt-data-foundation/05-02-SUMMARY.md
@.planning/phases/06-fabric-detail-pages/06-01-SUMMARY.md
@src/app/globals.css
@src/app/tecnologias/page.tsx
@src/lib/content/technologies.ts
@src/lib/content/helpers.ts
@src/components/tech-icon.tsx
@src/components/breadcrumb.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fade-in-up CSS animation and build Tecnologias page</name>
  <files>
    src/app/globals.css
    src/app/tecnologias/page.tsx
  </files>
  <action>
**globals.css**: Add a `fade-in-up` keyframe animation inside the `@theme` block (after the existing border-radius tokens, before the closing brace). Define:
```css
--animate-fade-in-up: fade-in-up 0.5s ease-out both;

@keyframes fade-in-up {
  from { opacity: 0; transform: translateY(0.75rem); }
  to { opacity: 1; transform: translateY(0); }
}
```

**tecnologias/page.tsx**: Rewrite the placeholder page as a Server Component (NO 'use client'). Structure:

1. **Metadata**: Keep existing title/description, update description to be more descriptive.

2. **Layout**: `mx-auto max-w-7xl px-4 lg:px-8 py-6 lg:py-10` (consistent with /uso/[slug]/page.tsx pattern).

3. **Breadcrumb**: `<Breadcrumb items={[{ label: 'Inicio', href: '/' }, { label: 'Tecnologias' }]} />`

4. **Page header**: Heading "Tecnologias Textiles" (h1, font-heading, 3xl/4xl) with a brief intro paragraph (1-2 sentences about Lafayette's textile technologies for school uniforms). Keep the intro SHORT and professional — this is Claude's discretion.

5. **Grid**: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6` displaying all 14 TECHNOLOGIES.

6. **Each card** (inline, no separate component file needed):
   - Container: `rounded-lg border border-border bg-background p-6 transition-all duration-300 hover:shadow-lg hover:-translate-y-1` with `animate-fade-in-up` class and inline `style={{ animationDelay: \`${i * 60}ms\` }}` for staggered entrance.
   - **Icon section**: Flex row with icon (TechIcon at size 28) inside a `size-12 rounded-lg bg-brand-primary/10 flex items-center justify-center` container, paired with `h3` name.
   - **Description**: `text-sm text-muted-foreground` paragraph. Use `tech.description` (NOTE: expandedDescription === description for all, so just use description).
   - **Cross-navigation section**: Below description, separated by `border-t border-border mt-4 pt-4`. Use `getFabricsByTechnology(tech.id)` to get associated fabrics. For each fabric, get its first category via `getCategoriesByFabric(fabric.id)[0]` for the link URL `/uso/{cat.id}/{fabric.id}`. Render as wrapped chip-links: `rounded-full bg-muted px-2.5 py-0.5 text-xs text-muted-foreground hover:bg-brand-primary hover:text-white transition-colors`.
   - **Fabric count limit**: If a technology has many fabrics (e.g., proteccion-solar has 30+), show all chips — they wrap naturally and the text is short. Do NOT truncate — the salesperson needs the full list.
   - Label above chips: "Telas con esta tecnologia" in `text-xs font-medium text-muted-foreground mb-2`.
   - Only show the cross-navigation section if `fabrics.length > 0`.

7. **Imports needed**: `Link` from next/link, `{ TECHNOLOGIES, getFabricsByTechnology, getCategoriesByFabric }` from @/lib/content, `{ TechIcon }` from @/components/tech-icon, `{ Breadcrumb }` from @/components/breadcrumb, `type { Metadata }` from next.

8. **Important**: Use `'use client'` is NOT needed. This is a pure Server Component. All data is static TypeScript imports. The CSS animations work without JavaScript.
  </action>
  <verify>Run `bun run build` — must complete with zero errors and /tecnologias route appears in the static pages output.</verify>
  <done>The /tecnologias page displays 14 technology cards in a responsive grid with icons, descriptions, and clickable fabric chip-links that navigate to the correct fabric detail pages. Cards animate in with staggered fade-in-up on page load.</done>
</task>

</tasks>

<verification>
1. `bun run build` passes with zero errors
2. `/tecnologias` appears in SSG output (static icon, not lambda)
3. Visual: 14 cards with icons, names, descriptions, and fabric chip-links
4. Click any fabric chip → navigates to correct /uso/{category}/{fabricId} page
5. Hover effects (shadow + elevation) work on each card
</verification>

<success_criteria>
- /tecnologias shows 14 technology cards with icons (PNG and Lucide fallbacks), names, and descriptions
- Each card lists associated fabrics as clickable chips linking to /uso/{cat}/{fabricId}
- Page has breadcrumb, responsive grid (1/2/3 cols), hover effects, and staggered entry animation
- Build passes with zero errors, page is SSG
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-section-pages/07-01-SUMMARY.md`
</output>
